v1:

Реализуй Implement B.

Перед началом:
1. Прочитай текущий Spec.
2. Прочитай Handoff после Implement A.
3. Составь короткое summary:
   - какие инварианты нельзя нарушать;
   - что реально уже реализовано;
   - есть ли расхождения между Spec и Handoff.

Правила:
- Handoff считать источником фактического состояния системы.
- Spec считать источником целевого замысла.
- Если есть конфликт между Spec и Handoff, явно укажи его.
- Не переопределяй решения из Implement A без явной причины.
- Перед кодом перечисли, на что именно опираешься.

----

v2 щаблон:
## Readback

### From Spec
(агент сам извлекает)

### From Handoff
(агент сам извлекает)

### Invariants
(агент обязан сформулировать)

v2 пример:

## Readback before Implement B

### From Spec
- нужно реализовать API layer
- формат ошибок единый
- auth уже считается существующим

### From Handoff A
- repository уже сделан
- PK = user_id
- soft delete через deleted_at
- naming = snake_case

### Invariants
- не менять schema
- не менять naming
- не ломать совместимость API